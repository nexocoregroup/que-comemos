<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|locale|smallestScreenSize|screenLayout|uiMode|navigation|density"
            android:name=".MainActivity"
            android:label="@string/title_activity_main"
            android:theme="@style/AppTheme.NoActionBarLaunch"
            android:launchMode="singleTask"
            android:exported="true">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

        </activity>

        <!-- Viene del andamiaje de Capacitor, no de un complemento nuestro: es
             la vía por la que el WebView entrega archivos al sistema sin
             exponer rutas. No pide permisos ni añade peso, así que se queda. -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths"></meta-data>
        </provider>
    </application>

    <!-- Permisos. Son estos dos y ninguno más: la app guarda todo en el propio
         teléfono, así que no tiene nada que pedir sobre fotos ni sobre
         archivos del usuario. -->

    <uses-permission android:name="android.permission.INTERNET" />

    <!-- Dictar. El reconocimiento corre dentro del aparato cuando el teléfono
         lo soporta, pero el micrófono sigue necesitando permiso del usuario. -->
    <uses-permission android:name="android.permission.RECORD_AUDIO" />

    <!-- Android 11 y posteriores esconden qué apps hay instaladas. Sin esto, el
         teléfono no encuentra su propio motor de reconocimiento de voz y el
         dictado falla sin decir por qué. -->
    <queries>
        <intent>
            <action android:name="android.speech.RecognitionService" />
        </intent>
    </queries>
</manifest>
